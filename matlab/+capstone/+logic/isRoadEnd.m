function truth = isRoadEnd(cfg,frame)
% Check if the switch condition of map is end
    % Check if input param is valid
    if isempty(cfg) || cfg == strunt()
        disp("isRoadEnd error: Need cfg")
        return
    end

    
end