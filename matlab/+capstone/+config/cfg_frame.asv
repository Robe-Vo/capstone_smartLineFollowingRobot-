function frame = cfg_frame()
    % Struture of each frame in running

    frame = struct([]);

    frame.line.raw       = zeros(5);
    frame.line.data      = zeros(5);
    frame.line.logicMask = zeros(5);

    frame.ultra.raw      = 0;
    frame.ultra.data     = 0;

    frame.mpu.gyro       = zeros()
end