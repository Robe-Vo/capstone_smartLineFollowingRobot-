function s = detectStability(rt, cfg) %#ok<INUSD>
    % Minimal placeholder: treat as NORMAL always.
    % App/logic có thể thay bằng tiêu chí dao động thật (variance error, sign flips, etc.)
    s = "NORMAL";
end